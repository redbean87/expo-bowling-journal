{
  "name": "sqlite-import-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env=\"\"",
    "dev:remote": "wrangler dev --remote --env=\"\"",
    "deploy": "wrangler deploy --env=\"\"",
    "deploy:production": "wrangler deploy --env production",
    "check": "tsc --noEmit",
    "logs": "wrangler tail --env=\"\""
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260204.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.2.0"
  },
  "dependencies": {
    "sql.js": "^1.13.0"
  }
}
