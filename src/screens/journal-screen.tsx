import { Text } from 'react-native';

import { PlaceholderScreen } from '@/components/placeholder-screen';

export default function JournalScreen() {
  return (
    <PlaceholderScreen
      title="Journal"
      subtitle="This tab will list leagues, sessions, and games once Convex and hooks are wired."
    >
      <Text>Upcoming: add game/session list and create game route.</Text>
    </PlaceholderScreen>
  );
}
